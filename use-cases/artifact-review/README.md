# Manual Artifact Review

Runs Kiro from `workflow_dispatch` and uploads normalized markdown/JSON artifacts without posting comments.

Authority: `read-only-artifact`

Install:

```bash
./install.sh artifact-review
```

This is the safest place to experiment with new Kiro agents before adding PR comments or gates.
